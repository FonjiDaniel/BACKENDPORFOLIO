const mongoose = require("mongoose");
const portfoliomodel = new mongoose.model("projects" ,  mongoose.Schema({

    _id: string,
    avatar: string,
    name: string,
    review: string,

}))


module.esports ={
    testimonialsModel
}